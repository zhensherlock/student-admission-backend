import { MidwayConfig } from '@midwayjs/core';

export default {
  // use for cookie sign key, should change to your own and keep security
  keys: 'StudentAdmissionPlatform',
  koa: {
    port: 7101,
  },
} as MidwayConfig;
